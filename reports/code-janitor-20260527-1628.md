# Code Janitor Report — Vocalie-TTS — 20260527-1628

## Context

- Commit: `04244da` on `main`
- Scope scanned: `backend/`, `ui_gradio/`, `frontend/src/`, `scripts/`, `tts_backends/`, root Python modules (`app.py`, `state_manager.py`, `session_manager.py`, `tts_engine.py`, `tts_pipeline.py`, `text_tools.py`), `.github/workflows/ci.yml`, docs/config files
- Constraints: no patches, no feature work

## Verdict

**READY_WITH_STRUCTURAL_SIGNALS**

Rationale:

- Surface-level entropy is **low**: 15 unused imports, 1 redefined function, 0 TODOs in critical paths, 0 debug prints in production backend/frontend, clean logging in canonical backend.
- The Gradio runner files (`tts_backends/xtts_runner.py`, `bark_runner.py`, etc.) use `print()` for error reporting — acceptable in subprocess runners but noted.
- Structural signals are present and significant (see below). These are not surface issues the janitor can clean; they require architectural attention.

## Surface findings (prioritized)

### JAN-01 — Unused imports (15 occurrences)
- Severity: LOW
- Evidence: ruff F401 reports: `json`, `typing.Optional`, `refs.DEFAULT_REF_DIR`, `state_manager.save_state`, `tts_backends.base.pick_default_language`, `ui_gradio.gradio_helpers.{coerce_float, coerce_int, is_under_dir, update_clean_preview}`, `backend.schemas.models.ModelInfo`, `backend.services.audiosr_service.datetime`, `backend.services.tts_service.{datetime, Iterable}`, `tts_engine.os`
- Risk: import noise, slight cognitive load in large files
- Effort: S (auto-fixable with `ruff --fix`)

### JAN-02 — Redefined function: `count_words` in `text_tools.py`
- Severity: LOW
- Evidence: F811 — `count_words` is defined at line 70 and redefined at line 196 with identical implementation
- Risk: shadowing, confusion; both implementations are identical so no behavior divergence
- Effort: S (remove the second definition)

### JAN-03 — Stale extraction residue in `app.py`
- Severity: LOW
- Evidence: Lines 638 and 687 contain comment-only placeholders (`# _coerce_float / _coerce_bool moved to ui_gradio.gradio_helpers`) where functions used to be; these are harmless but could be consolidated
- Risk: negligible
- Effort: S

### JAN-04 — Orphan script `tts_test.py`
- Severity: LOW
- Evidence: Root-level `tts_test.py` (ad-hoc Chatterbox weight test, no imports from it anywhere)
- Risk: confusion about whether it's part of the test suite
- Effort: S (move to scripts/ or remove)

### JAN-05 — `gilles` HTML blob in repo root
- Severity: LOW
- Evidence: 80KB HTML file at root, unclear origin/purpose, not referenced by code
- Risk: repo bloat, confusion
- Effort: S (remove or move to docs/ if historical reference)

### JAN-06 — `security_best_practices_report.md` in root instead of `reports/`
- Severity: LOW
- Evidence: Historical report at root, should be in `reports/` per convention (reports are immutable evidence)
- Risk: convention drift
- Effort: S (move + gitignore)

### JAN-07 — `openapi.json` checked into repo
- Severity: LOW
- Evidence: 2771-line auto-generated OpenAPI spec committed at commit `1c8b8ef`
- Risk: staleness risk if not regenerated on API changes; `scripts/update-openapi.sh` exists
- Effort: S (add to .gitignore or make regeneration CI-guarded)

### JAN-08 — `print()` in TTS backend runners (not production backend)
- Severity: INFO
- Evidence: `xtts_runner.py`, `xtts_prefetch.py`, `bark_prefetch.py`, `bark_backend.py`, `piper_runner.py` use `print()` for error/status output
- Risk: these run as subprocesses where structured logging is impractical; acceptable
- Effort: N/A (no change needed)

## Structural signals (carried forward to tech-debt pass)

1. **Dual orchestration surface**: `app.py` (2978 lines, 25 handle_ functions) + `backend/` (canonical API) coexist. `backend/services/tts_service.py` imports 7 root-level modules. This is not surface noise — it's architectural duplication.

2. **Triple-computed path constants**: `WORK_DIR`, `OUTPUT_DIR`/`DEFAULT_OUTPUT_DIR` are independently computed in `backend/config.py`, `app.py`, and `ui_gradio/gradio_helpers.py` from the same env vars. Same logic, three locations.

3. **Dual `clean_work_dir`**: `app.py` version has BASE_DIR safety check + logging; `backend/services/work_service.py` version lacks both. Semantic divergence.

4. **Legacy preset bridge in canonical backend**: `backend/services/preset_service.py` contains `_legacy_to_ui_state()` with hardcoded engine alias map (`chatterbox` → `chatterbox_finetune_fr`). Canonical backend actively translates legacy shapes.

5. **In-memory job store with no persistence**: `JOB_STORE` is a process-scoped dict. Backend restart loses all job state. `_JOB_STATE`/`_JOB_LOCK` in `app.py` is a separate parallel mechanism.

6. **Backend boundary violations**: `backend/routes/tts.py`, `backend/services/tts_service.py`, `backend/routes/audio.py`, etc. import from root-level modules (`refs`, `text_tools`, `audio_defaults`, `output_paths`, `session_manager`, `tts_pipeline`). The canonical backend is not self-contained.

7. **Frontend monolith**: `page.tsx` at 1675 lines with 34 `useState` hooks. Single-component architecture.

## Actions correctives recommandees (surface only)

- `JAN-01`: `ruff check --fix --select F401,F811` then review
- `JAN-02`: Remove duplicate `count_words` from `text_tools.py` (keep line 70, remove line 196)
- `JAN-03`–`JAN-07`: Minor hygiene moves listed above